services:
  nowen-forge:
    build: .
    container_name: nowen-forge
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
      DATA_DIR: /app/data
      PORT: 3001
      HOST: 0.0.0.0
    volumes:
      - ./data:/app/data
